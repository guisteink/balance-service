#!/usr/bin/env groovy
pipeline {
    agent none

    tools { nodejs "node" }

    stages {
        stage('Build') {
            // agent {
            //     docker { image 'node'}
            // }
            steps {
                sh "npm start"
            }
        }
    }
}
